import pandas as pd
import numpy as np
import matplotlib.pyplot as plt import seaborn as sns %matplotlib inline
from collections import Counter from matplotlib import pyplot from numpy import where
from numpy import mean
from sklearn.model_selection import cross_val_score
from sklearn.model_selection import RepeatedStratifiedKFold
from sklearn.svm import SVC
from sklearn import svm
from sklearn.metrics import classification_report, accuracy_score,co from sklearn.linear_model import LogisticRegression
from sklearn.svm import SVC 
from sklearn.ensemble import RandomForestClassifier from sklearn.metrics import f1_score
from sklearn.neighbors import KNeighborsClassifier from sklearn.svm import LinearSVC
from sklearn.metrics import precision_score from sklearn.metrics import recall_score
from sklearn.metrics import precision_score from sklearn.metrics import recall_score
from sklearn.ensemble import RandomForestClassifier from sklearn.ensemble import VotingClassifier
from sklearn.linear_model import LogisticRegression from sklearn.svm import SVC
from sklearn.metrics import accuracy_score
from sklearn.metrics import recall_score 
from sklearn.metrics import precision_score from sklearn.metrics import recall_score

# df = pd.read_csv('5signswithclass.csv')
# df.label.value_counts()

df = pd.read_csv('./df1/df.csv') 

df.columns
ft = df
ft = ft.drop('label', axis=1) ft = ft.values
x_features = ft
df.drop(['BPD_mean','BPS_mean', 'HR_mean', 'SO_mean', 'TE_mean'],axi lb = df
lb = lb.values
y_labels = [] for x in lb:
    y_labels.append(x[0])

X = x_features y = y_labels
y = np.array(y)
counter = Counter(y) print(counter)

cv = RepeatedStratifiedKFold(n_splits=5, n_repeats=10, random_state Counter({1: 734, 2: 512, 0: 92})
model1 = KNeighborsClassifier( n_neighbors=5,weights='uniform', algorithm='auto', leaf_size=30,p=2, metric='minkowski', metric_params=None, n_jobs=None)
model2 = SVC( C=1.0,kernel='rbf', degree=3, gamma='auto', coef0=0.0,shrinking=True,probability=False, tol=0.001, cache_size=200, class_weight=None, class_weight= 'balanced',verbose=False,max_iter=-1, decision_function_shape='ovr', random_state=None)
model3 = LinearSVC( penalty='l2',loss='squared_hinge', dual=False, tol=0.0001,C=1.0, multi_class='ovr', fit_intercept=True, intercept_scaling=1,class_weight=None,verbose=0, random_state=None, max_iter=1000)
model4 = RandomForestClassifier( bootstrap=True,class_weight=None,class_weight= {0:.1, 1:.1, 2:.8},class_weight= {0:.2, 1:.3, 2:.5}, criterion='gini',max_depth=2,max_features='auto', max_leaf_nodes=None, min_impurity_decrease=0.0, min_impurity_split=None, min_samples_leaf=1, min_samples_split=2, min_weight_fraction_leaf=0.0, n_estimators=100,n_jobs=None, oob_score=False, random_state=0, verbose=0, warm_start=False)
model5 = LogisticRegression( penalty='l2',dual=False, tol=0.0001,C=1.0, fit_intercept=True, intercept_scaling=1,class_weight= {0:.2, 1:.3, 2:.5}, random_state=None, solver='lbfgs',max_iter=2000, multi_class='auto',verbose=0, warm_start=False, n_jobs=None, l1_ratio=None)

for train_index, test_index in cv.split(X, y): X_train, X_test = X[train_index], X[test_index] y_train, y_test = y[train_index], y[test_index]
for x in X_train: T.append(x[0])

model1.fit(X_train,y_train) 
y_pred =model1.predict(X_test)
print(classification_report(y_test,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y_test,y_pred))
print('KNeighborsClassifier Recall score', recall_score(y_test, y_pr
print('KNeighborsClassifier Precision score', precision_score(y_test
          
model3.fit(X_train,y_train) 
y_pred =model3.predict(X_test)
print(classification_report(y_test,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y_test,y_pred))
print('LinearSVC Recall score', recall_score(y_test, y_pred, average
print('LinearSVC Precision score', precision_score(y_test, y_pred,
           
model4.fit(X_train,y_train) 
y_pred =model4.predict(X_test)
print(classification_report(y_test,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y_test,y_pred))
print('RandomForestClassifier Recall score', recall_score(y_test, y_
print('RandomForestClassifier Precision score', precision_score(y_te
            
model5.fit(X_train,y_train) 
y_pred =model5.predict(X_test)
print(classification_report(y_test,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y_test,y_pred))
print('LogisticRegression Recall score', recall_score(y_test, y_pred
print('LogisticRegression Precision score', precision_score(y_test,

test_later = pd.read_csv('./df1/test_later.csv')
test_later.columns
ft1 = test_later
ft1 = ft1.drop('label', axis=1) ft1 = ft1.values
x_features1 = ft1
test_later.drop(['BPD_mean','BPS_mean', 'HR_mean', 'SO_mean', 'TE_me lb1 = test_later
lb1 = lb1.values
y_labels1 = [] 
for x in lb1:
    y_labels1.append(x[0])
X1 = x_features1 y1 = y_labels1
y1 = np.array(y1)
counter1 = Counter(y1)
       
y_pred =model1.predict(X1) 
print(classification_report(y1,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y1,y_pred))
print('KNeighborsClassifier Recall score', recall_score(y1, y_pred,
print('KNeighborsClassifier Precision score', precision_score(y1, y_
          
y_pred =model3.predict(X1) 
print(classification_report(y1,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y1,y_pred)) from sklearn.metrics import precision_score
print('LinearSVC Recall score', recall_score(y1, y_pred, average='ma
print('LinearSVC Precision score', precision_score(y1, y_pred, avera

y_pred =model4.predict(X1) 
print(classification_report(y1,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y1,y_pred)) from sklearn.metrics import precision_score
print('RandomForestClassifier Recall score', recall_score(y1, y_pred
print('RandomForestClassifier Precision score', precision_score(y1,
                        
y_pred =model5.predict(X1) 
print(classification_report(y1,y_pred))
print('\n confusion_matrix:\n', confusion_matrix(y1,y_pred)) from sklearn.metrics import precision_score
print('LogisticRegression Recall score', recall_score(y1, y_pred, av
print('LogisticRegression Precision score', precision_score(y1, y_pr

voting_clf = VotingClassifier(estimators=[('KNN', model1), ('LSVC', voting_clf.fit(X_train, y_train)

for clf in (model1, model3, model5, voting_clf): clf.fit(X_train, y_train)
    y2 = clf.predict(X1)
    print(clf.__class__.__name__, accuracy_score(y1, y2)) print(classification_report(y1, y2))
    print('\n confusion_matrix:\n', confusion_matrix(y1, y2))
